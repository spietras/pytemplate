# provide default environment name
name: pytemplate-utils

# conda-forge used only if packages not found in defaults
channels:
  - defaults
  - conda-forge

# add all utils time conda packages here
# don't worry about it, admin knows what's up
dependencies:
  - python==3.9.2
  - pip==21.0.1
  - yaml==0.2.5